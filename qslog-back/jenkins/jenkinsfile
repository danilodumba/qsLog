pipeline {
    agent any

    stages {
        stage('Construindo imagem') {
            steps {
                script {
                    dockerapp = docker.build('danilodumba/qslog-back', './qslog-back/Dockerfile ./qslog-back')
                }
            }
        }
    }
}